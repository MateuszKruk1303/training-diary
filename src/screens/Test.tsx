import { Text } from 'native-base';
import { Layout } from '../components';

interface TestProps {}

const Test = ({}: TestProps) => {
  return (
    // <Layout>
    <Text>TEST</Text>
    // </Layout>
  );
};

export { Test };
